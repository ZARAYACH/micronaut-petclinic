# Oracle db Configuration Profile
datasources:
  default:
    db-type: oracle
    db-name: petclinic
    schema-generate: CREATE_DROP
    dialect: ORACLE
    initialization-fail-timeout: 60000
    connection-timeout: 30000
    maximum-pool-size: 10
    minimum-idle: 2
